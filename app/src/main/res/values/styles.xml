<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    <style name="FabMenuStyle">
        <item name="android:background">@drawable/fab_label_background</item>
        <item name="android:textColor">#000000</item>
    </style>
    <style name="StartTheme" parent="android:Theme.Translucent.NoTitleBar.Fullscreen">
        <item name="windowNoTitle">true</item>

    </style>
    <style name="popup_anim">
        <item name="android:windowEnterAnimation">@anim/popup_anim_up</item>
        <item name="android:windowExitAnimation">@anim/popup_anim_down</item>
    </style>
</resources>
